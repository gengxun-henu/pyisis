# M05 New Session Prompt

> Convenience copy only. The milestone registry and `task_plan.md`,
> `findings.md`, and `progress.md` remain the source of truth.

在工作树 `D:\code\pyisis\pyisis\.worktrees\m04-windows-pyisis-wheelhouse` 中，使用 `$milestone-session-manager`、`$planning-with-files` 和 `$superpowers:brainstorming` 开始里程碑 `windows-isis9-m05-native-app-design`。先读取适用的 `AGENTS.md`、`.planning/milestones.v1.json`，以及 `.planning/windows-isis9-m05-native-app-design/` 下的 `task_plan.md`、`findings.md`、`progress.md`；再核对 worktree/branch、Git 状态、最近提交、M04 归档完成证据和 ISIS 9 前置路径。运行里程碑验证并激活 M05，只推进现有规划记录的唯一 Next Step：通过头脑风暴确定原生 Windows ISIS APP 发布包格式和明确的首发 APP 清单（至少包含 reduce、jigsaw、qnet）。在设计获批前，不构建或发布 APP 包，不把独立 APP 可执行文件或 XML 放进 PyISIS wheels，不修改 `.gitignore` 或 `print.prt`。
