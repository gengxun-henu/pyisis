
什么时候应该优先用 lambda 包装？
一般这几种情况非常适合：

1. 需要改参数或返回值类型
比如 Qt / STL / Python 容器之间转换。

2. 需要合并或拆分参数
例如 C++ 有多个输出参数，你想在 Python 返回 tuple。

3. 需要处理重载
不想写复杂的成员函数指针类型推导。

4. 需要控制生命周期
不想直接暴露引用、指针、内部状态。

5. 需要做异常、空值、边界情况处理
例如空指针转 None。

6. 想做 Python 风格 API
包括命名、默认值、行为语义适配。

你的 get_surface_point 就属于第 4 类非常典型的例子，也顺带有第 2 层“结果标准化”的意味。

什么时候不一定需要 lambda？
如果一个函数满足下面条件，通常可以直接绑定成员函数指针：

没有重载歧义
参数类型 pybind11 能直接懂
返回值类型 pybind11 能直接懂
生命周期没风险
不需要改名字以外的任何逻辑
例如：

这就很适合直接绑。

所以你可以把规则记成一句话：

直接绑定适合“原函数已经适合 Python”；lambda 包装适合“需要中间适配”。